# The base URL for the API in development.
# This is intentionally left blank because we are using the Vite proxy.
# Requests will be made to relative paths like /api/v1/... and Vite will proxy them.
VITE_API_BASE_URL=http://127.0.0.1:8080